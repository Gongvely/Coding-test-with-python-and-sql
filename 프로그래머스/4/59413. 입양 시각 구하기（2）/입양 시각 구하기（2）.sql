WITH RECURSIVE HOURS AS(
SELECT 0 AS N
UNION ALL
SELECT N+1 
FROM HOURS
WHERE N < 23
)

SELECT H.N HOUR, COUNT(A.ANIMAL_ID) COUNT
FROM HOURS H
LEFT JOIN  ANIMAL_OUTS A
ON H.N = HOUR(A.DATETIME)
GROUP BY H.N

